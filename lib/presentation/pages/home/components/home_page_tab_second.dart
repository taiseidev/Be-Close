import 'package:flutter/material.dart';

class HomePageTabSecond extends StatelessWidget {
  const HomePageTabSecond({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Hello World",
      ),
    );
  }
}
