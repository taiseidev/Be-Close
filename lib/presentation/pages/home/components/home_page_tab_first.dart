import 'package:flutter/material.dart';

class HomePageTabFirst extends StatelessWidget {
  const HomePageTabFirst({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Hello World",
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}
